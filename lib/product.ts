// The single source of truth for the Wieman OS trial funnel. Always a plain
// link-out — never an iframe or a cross-origin fetch; the site's CSP stays
// strict. Shared by /wieman-os and the homepage product band.
export const TRIAL_URL = "https://try.wiemansystems.com";
